import 'package:flutter/material.dart';

class AppColors {
  // --- ShopLuxe Premium Palette ---

  // Primary: گہرا سیاہ (پریمیم برانڈنگ کے لیے)
  static const Color primary = Color(0xFF000000);

  // Secondary: کبھی کبھی ایکسنٹ کے لیے گولڈ یا گہرا گرے استعمال ہوتا ہے
  static const Color secondary = Color(0xFF333333);

  // Backgrounds
  static const Color scaffoldBackground = Color(0xFFF9F9F9); // بہت ہلکا گرے (تاکہ کارڈز ابھر کر آئیں)
  static const Color surface = Color(0xFFFFFFFF); // کارڈز کا رنگ (سفید)

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A); // تقریبا سیاہ (پڑھنے میں آسان)
  static const Color textSecondary = Color(0xFF757575); // گرے (تفصیلات کے لیے)

  // Action Colors
  static const Color error = Color(0xFFD32F2F); // سرخ (Error یا Sale کے لیے)
  static const Color success = Color(0xFF388E3C); // سبز (Success message)
  static const Color warning = Color(0xFFFBC02D); // پیلا (Star ratings)

  // Borders & Dividers
  static const Color border = Color(0xFFE0E0E0); // ہلکی لکیریں
  static const Color inputFill = Color(0xFFF5F5F5); // ان پٹ فیلڈ کا بیک گراؤنڈ
}